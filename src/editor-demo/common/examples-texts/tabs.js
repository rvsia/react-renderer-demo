import React from 'react';
import Typography from '@material-ui/core/Typography';

export default <Typography>
  <p>For using this component, you have to append TabItem component (containing form fields) to fields property.</p>
  <Typography variant="h6" gutterBottom>
    Component constant
  </Typography>
  <pre>TAB_ITEM</pre> as an import from componentTypes
  <pre>tab-item</pre> as a string
</Typography>;
